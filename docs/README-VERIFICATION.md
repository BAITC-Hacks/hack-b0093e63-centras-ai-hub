# Верификация README — журнал проверки

- **Дата проверки:** 2026-09-23
- **Проверенный коммит:** [`64167ec`](../../commit/64167ece497609e854288e6184ba0bd998777701)
  (`fix(chat): no claimed actions without tools, canonical navigate URLs, less narration`) —
  это был HEAD ветки `feat/ekt-ai-consultant` в момент старта проверки. Все команды выполнялись
  в **чистом клоне** этого коммита (`git clone` в отдельную временную папку), не в рабочей
  копии.
- Пока шла проверка, другие агенты продолжали коммитить в эту же ветку (расширение Chrome,
  e2e), HEAD ветки ушёл вперёд (на момент записи этого файла — `ee82d5a`). Ниже фиксируется
  состояние ровно коммита `64167ec`; описание браузерного расширения в README сознательно не
  добавлялось — это отдельная, ещё не завершённая работа.
- **Проверяющий:** агент-жюри (Claude), по заданию пользователя (alphacentras@gmail.com).
- **Как проверялось:** клон → свой `.env.local` (значения `SUPABASE_URL`,
  `SUPABASE_SERVICE_ROLE_KEY`, `OPENAI_API_KEY` скопированы из `.env.local` основного репозитория
  через отдельный Node-скрипт, без вывода значений в лог) → команды README по порядку. Локальный
  Docker-стек (`npm run setup:local`) и полный обход (`npm run scrape`), GitHub Actions и Vercel
  **не запускались** — по прямому решению пользователя (работа только с облачным Supabase).
  Клон удалён после проверки.

## Итоговые счётчики

| Результат | Количество |
|---|---|
| OK | 34 |
| WRONG (исправлено) | 3 |
| NOT VERIFIABLE (без запуска, по решению пользователя) | 4 |

## Проверка 11 обязательных разделов README

| № | Раздел | Есть в README | Чем подтверждён |
|---|---|---|---|
| 1 | Название проекта | да, `## 1. Название проекта` | соответствует заголовку README и `package.json.name` |
| 2 | Краткое описание (проблема, для кого) | да, `## 2. Краткое описание` | текстовое описание, не требует запуска кода |
| 3 | Что реализовано | да, `## 3. Что реализовано` | каждый пункт перепроверен отдельно ниже (тесты, виджет, поиск, цены и т.д.) |
| 4 | Как работает (сценарий вход → результат) | да, `## 4. Как работает решение` | подтверждено вручную вопросом «Нужна светодиодная лампа E27…» — пройден весь сценарий: session → статус «Ищу товары…» → RPC поиска → потоковый ответ → карточки товаров |
| 5 | Технологии | да, `## 5. Технологии` | версии/названия сверены с `package.json`, `supabase/config.toml`, `web/build.mjs` |
| 6 | Архитектура (компоненты и связи) | да, `## 6. Архитектура проекта` + «Структура репозитория» | все перечисленные пути и файлы существуют в репозитории (см. таблицу ниже) |
| 7 | Установка и запуск (пошагово, команды) | да, `## 7. Установка и запуск`, пути A/B/C | путь A и часть пути C выполнены в чистом клоне (см. таблицу команд); путь B (Docker) не запускался по решению пользователя |
| 8 | Как проверить (сценарий для жюри) | да, `## 8. Как проверить решение` | все под-пункты 8.1–8.5 выполнены в клоне, см. таблицу команд |
| 9 | Данные и интеграции | да, `## 9. Данные и интеграции` | числа сверены с `seed/*.jsonl`, `seed/scrape-report.json`, RPC `search_products` |
| 10 | Ограничения | да, `## 10. Ограничения` | пункты 1 (6 кнопок демо, 2 про незагруженные категории), 7 (Vercel не задеплоен), 8 (Docker не прогнан целиком), 11 (нет уведомлений по заявкам) — все перепроверены и подтвердились |
| 11 | Ссылка на deployed-версию | да, `## 11. Ссылка на deployed-версию` | `GET /chat/health` и `POST /chat` реально отвечают (см. таблицу команд) |

Все 11 разделов присутствуют, в правильном порядке, содержимое не удалялось — только
исправлены неверные числа и дополнены места, не описанные в README (см. «Список правок» ниже).

## Таблица проверок: команды и заявленные факты

Легенда: **OK** — команда/число подтвердились как есть; **WRONG** — было неверно, README
исправлен; **NOT VERIFIABLE** — не запускалось в этой проверке (см. причину).

| # | Claim / команда | Результат | Примечание |
|---|---|---|---|
| 1 | `git clone` ветки `feat/ekt-ai-consultant` | OK | клон коммита `64167ec` создан без ошибок |
| 2 | `npm ci` (Путь A/B/C, README §7) | **WRONG** | на чистом клоне падает: `npm error Missing: happy-dom@20.14.5 from lock file` (и ещё 6 транзитивных пакетов). `package-lock.json` коммита `64167ec` не соответствует `package.json` (там же). `npm install` работает и ставит рабочий `node_modules` (92 пакета). Добавлена сноска в README (см. «Список правок») |
| 3 | Node.js ≥ 20 (`engines.node` в `package.json`) | OK | `"engines": {"node": ">=20"}`, локально Node 24.14.0 |
| 4 | Node-однострочник из шапки README (health-запрос к `/chat`) | OK | выполнен дважды (curl-эквивалент и сам однострочник); ответ содержит LEDVANCE 081100328_ 620 ₸ и PHILIPS 080300163_ 800 ₸ среди товаров, как заявлено |
| 5 | `curl .../chat/health` → `{"ok":true,"db":true,"openai_key":true,"model":"gpt-4.1-mini",...}` | OK | ответ дословно совпал (плюс `"time"`) |
| 6 | `npm run dev:web` поднимает демо-страницу и `widget.js` на :5173 | OK | `GET /` → 200, `GET /widget.js` → 200 |
| 7 | `npm test` → «113 тестов» (README §3, §8.4) | OK | `vitest run`: 5 файлов, **113 passed (113)** — число совпало точно |
| 8 | `npx -y deno@2 test supabase/functions/chat/` → «34 теста» (README §3, §8.4) | **WRONG** | реально **94 passed \| 0 failed** (http, openai, prompt, sse, tools, validate, ui_actions). README исправлен на 94 |
| 9 | `npx tsc --noEmit -p .` / `npm run typecheck` | OK | завершается без ошибок и без вывода (exit 0) |
| 10 | `npm run build:web` → «38.5 KB» (README §3, §5) | OK | вывод: `web\public\widget.js 38.5kb`, скрипт печатает `38.5 KB (gzip 14.5 KB), limit 40.0 KB` — точное совпадение, gzip-цифра добавлена в README |
| 11 | `npm run smoke` против развёрнутого API → «5/5 PASS» (README §8.2) | OK | реально 5/5 PASS, те же 5 проверок (health, лампа E27, три невалидных запроса → 400) |
| 12 | `npm run check:search` (не был в README) | **добавлено в README** | реально 6/6 PASS против живого RPC `ekt.search_products`; скрипт существует (`scripts/check-search.mjs`), но отсутствовал в таблице npm-скриптов — дополнено |
| 13 | `npm run eval -- --only <id1>,<id2>` (2 кейса, не полный прогон) | OK | `pick-lamp-e14-candle-warm` и `branch-astana-address` — оба **PASS**, синтаксис `--only` с запятой работает |
| 14 | `npm run ingest -- --dry-run --dir seed` | OK | завершился без обращения к БД, напечатал «чанки: 539» — совпадает с заявленными «539 фрагментов» (README §9) |
| 15 | `npm run ingest:seed -- --dry-run` | OK | тот же результат (539 чанков, `--dry-run: в БД ничего не записано`) |
| 16 | Манual-таблица §8.3, вопрос 1 (лампа E27, тёплый свет, до 1000 ₸) | OK | ответ содержит LEDVANCE 081100328_ 620 ₸ и PHILIPS 080300163_ 800 ₸ со ссылками на ekt.kz, карточки товаров пришли |
| 17 | Манual-таблица §8.3, вопрос 2 (артикул 081100328_) | OK | ответ: 620 ₸ на сайте, 615 ₸ в магазине — совпадает и с README, и с `seed/products.jsonl` |
| 18 | Манual-таблица §8.3, вопрос 3 (условия возврата) | OK | 14 дней, без дефектов, полная комплектация, оригинальная упаковка, экспертиза, список невозвратных товаров — всё есть в ответе |
| 19 | Манual-таблица §8.3, вопрос 4 (филиал Астана, суббота) | OK | адрес, телефоны, суббота 09:00–13:00 — совпадает |
| 20 | Манual-таблица §8.3, вопрос 5 (рецепт борща) | OK | вежливый отказ, инструменты не вызваны (`products: none`) |
| 21 | Манual-таблица §8.3, вопрос 6 (автомат 25А) | OK | автоматов не нашёл, предлагает уточнить бренд/характеристики, ничего не выдумал |
| 22 | Снимок данных: 89 товаров, 24 категории, 123 страницы, 9 филиалов (README §9) | OK | посчитано напрямую по `seed/*.jsonl`/`seed/branches.json`: 89/24/123/9 — точное совпадение |
| 23 | `eval/questions.json` «около 40 сценариев» (README §8.5, §9) | OK (приблизительно) | реально 42 сценария — «около 40» не искажает факт, число не менялось |
| 24 | `eval/RESULTS.md`: пройдено 41/42 = 97.6%, порог 80% | OK | файл существует, README ссылается на него и не завышает точный процент |
| 25 | Порог eval «меньше 80% → exit 1» (`eval/run.ts`) | OK | в коде: `process.exit(rate < 0.8 ? 1 : 0)` |
| 26 | 3 миграции в разделе «База данных» (README справочник) | **WRONG (неполно)** | в репозитории 5 файлов миграций, README описывал только 3 (`init`, `order_note`, `price_history`); не хватало `20260923160000_search_recall.sql` и `20260923170000_search_rank.sql`. Дополнено (без удаления существующего текста) |
| 27 | Таблицы БД в README (`categories`, `products`, `price_history`, `pages`, `page_chunks`, `branches`, `chat_sessions`, `chat_messages`, `leads`, `feedback`, `scrape_runs`) | OK | все 11 таблиц реально созданы (`CREATE TABLE ekt.*`) в перечисленных миграциях |
| 28 | 7 инструментов LLM (`search_products`, `get_product`, `category_facets`, `list_categories`, `search_knowledge`, `get_branches`, `create_lead`) | OK | ровно 7 объявлений `name:` в `supabase/functions/chat/tools.ts` |
| 29 | «12 правил» в системном промпте (README справочник) | OK | в `prompt.ts` ровно 12 пронумерованных «Правил точности» (1–12) |
| 30 | Переменные Edge Function и их значения по умолчанию (README «Эксплуатация») | OK | `config.ts`: `OPENAI_MODEL` → `gpt-4.1-mini`, `OPENAI_EMBEDDING_MODEL` → `text-embedding-3-small`, `OPENAI_BASE_URL` → `https://api.openai.com/v1`, `OPENAI_MAX_COMPLETION_TOKENS` → `3000`, `ALLOWED_ORIGINS` → `*` — совпадает дословно |
| 31 | `vercel.json`: build `npm run build:web`, output `web/public` (README «Деплой в продакшен») | OK | `buildCommand`/`outputDirectory` в `vercel.json` совпадают дословно |
| 32 | `refresh-data.yml`: cron `15 22 * * *`, шаги `npm ci → npm test → scrape → ingest`, `workflow_dispatch.limit` | OK | всё совпадает дословно с файлом workflow (сам workflow не запускался — GitHub Actions вне рамок проверки) |
| 33 | Демо-страница: 6 кнопок-примеров, 2 из них про автомат 25А и кабель ВВГнг (README §10, п.1) | OK | `web/public/index.html`: ровно 6 `data-ask=`, среди них «Нужен автомат на 25А…» и «Сравни кабель ВВГнг…» |
| 34 | Пути к файлам/ссылки в README (миграции, `prompt.ts`, `eval/RESULTS.md`, `seed/README.md`, `.github/workflows/*.yml`, `vercel.json`, `scraper/src/*`, `scripts/*`, `web/build.mjs`, `web/dev/mock-server.mjs`, `.env.example`, `seed/*`) | OK | все перечисленные пути существуют в репозитории |
| 35 | `npm run setup:local` (Путь B, Docker) | NOT VERIFIABLE | не запускалось — решение пользователя: проверка только против облачного Supabase. README уже помечает путь B как локальный/опциональный, облачный путь (A) — основной |
| 36 | `npm run scrape` (полный обход ~14 000 URL) | NOT VERIFIABLE | не запускалось — заняло бы часы; README прямо описывает это ограничение (§9, §10 п.5) |
| 37 | GitHub Actions (реальный прогон `ci.yml`/`refresh-data.yml`) | NOT VERIFIABLE | вне рамок проверки; README и так честно пишет, что секреты `refresh-data.yml` не заданы (§10 п.4) |
| 38 | Vercel-деплой | NOT VERIFIABLE | вне рамок проверки; README и так честно пишет «пока не задеплоен» (§7, §10 п.7, §11) |

## Список правок в README.md

1. **§3 и §8.4:** число тестов `deno test` изменено с «34» на фактические **94** (файлы
   `http_test.ts`, `openai_test.ts`, `prompt_test.ts`, `sse_test.ts`, `tools_test.ts`,
   `ui_actions_test.ts`, `validate_test.ts`).
2. **§7 (перед Путь A):** добавлена сноска о том, что `npm ci` на момент проверки падает
   из-за рассинхронизации `package-lock.json`/`package.json` (лечится `npm install`);
   старый текст про `npm ci` не удалён, команда остаётся основной для CI.
3. **npm-скрипты (§7):** добавлена строка `npm run check:search` (скрипт существовал в
   `package.json`, но не был документирован).
4. **§8.4:** добавлена явная команда `npx tsc --noEmit -p .` рядом с `npm run typecheck`
   (это одна и та же проверка, но в README раньше был только алиас).
5. **«База данных» (справочник):** дописаны недостающие миграции
   `20260923160000_search_recall.sql` и `20260923170000_search_rank.sql` — README раньше
   упоминал только первые три файла из пяти существующих.
6. **§3:** к размеру виджета «38.5 KB» дописана фактическая цифра после gzip (14.5 KB) и
   лимит сборки (40 KB) — эта информация уже печатается скриптом сборки, но не была в README.

Ничего из существующего текста README не удалено и не сокращено — только исправлены два
неверных числа (34→94 теста; список миграций дополнен) и добавлена информация о поведении
`npm ci`, о `check:search` и о точном размере виджета.
